<controls:ActionUserControl x:Class="Sitter.ToolBox.Controls.FakeMongoData"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Sitter.ToolBox.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
            <RadioButton Margin="5" Name="rbDropAllCollections" IsChecked="True"  Unchecked="ChkDropMethods_OnChecked" Checked="ChkDropMethods_OnChecked">Drop mysitterhub database</RadioButton>
            <RadioButton Margin="5" Name="rbCreate" IsChecked="True"  Unchecked="RbCreate_OnUnchecked" Checked="RbCreate_OnUnchecked">Create Job Ready for Payment</RadioButton>

        </StackPanel>
    </Grid>
</controls:ActionUserControl>